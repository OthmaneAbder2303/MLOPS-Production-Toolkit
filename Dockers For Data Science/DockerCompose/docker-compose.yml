services:
  web:
    build: .
    ports:
      - "8080:5000"
    depends_on:
      - redis
  redis:
    image: "redis/redis-stack-server:latest"
    ports:
    - "6379:6379"  # This maps the Redis port to your Mac